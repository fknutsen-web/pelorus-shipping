import type { Metadata } from "next";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: {
    default: "Marine IQ — Maritime Intelligence & Reputation Network",
    template: "%s | Marine IQ",
  },
  description:
    "Verified maritime professionals review companies, software and conferences. A maritime trust, intelligence and reputation network — not a generic review site.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="flex min-h-screen flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
